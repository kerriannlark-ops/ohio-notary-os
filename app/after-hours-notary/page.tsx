import { PublicHero } from "@/components/public-hero";

export default function AfterHoursNotaryPage() {
  return (
    <PublicHero title="After-hours notary support for evenings and weekends." subtitle="After-hours requests stay line-itemed so convenience pricing remains separate from Ohio statutory act fees." />
  );
}
