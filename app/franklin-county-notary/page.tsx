import { PublicHero } from "@/components/public-hero";

export default function FranklinCountyNotaryPage() {
  return (
    <PublicHero title="Franklin County mobile notary coverage with zip-aware travel quoting." subtitle="Travel zones, urgency, and specialty visits are quoted separately so public visitors understand the full appointment cost." />
  );
}
