import { SectionCard } from "@/components/section-card";

export default function TermsPage() {
  return (
    <SectionCard title="Terms" eyebrow="Portal terms">
      <p className="text-sm text-walnut/75">Portal use, disclosure acceptance, and scheduling terms are stored separately from internal admin notes.</p>
    </SectionCard>
  );
}
